export interface ErrorType {
  required: string;
  invalidNumber: string;
  invalidAlphanumber: string;
  invalidCero: string;
  minlength: string;
  maxlength: string;
  emptySpace: string;
  min: string;
  max: string;
}
